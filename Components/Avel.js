import { StyleSheet, View } from "react-native";

export default function Avel() {
  return <View style={styles.qwert}></View>;
}

const styles = StyleSheet.create({
  qwert: {
    width: "100%",
    height: 600,
  },
});
